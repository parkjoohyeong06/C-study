#include <stdio.h>

int my_strcmp(char *s1, char *s2) {
    int i = 0;

    while (s1[i] != '\0' || s2[i] != '\0') {
        if (s1[i] > s2[i]) return 1;    // s1이 더 큼
        if (s1[i] < s2[i]) return -1;   // s1이 더 작음
        i++;
    }

    return 0;  // 문자열이 완전히 같은 경우
}

int main() {
    char str1[100], str2[100];

    scanf("%s", str1);
    scanf("%s", str2);

    printf("%d", my_strcmp(str1, str2));

    return 0;
}
