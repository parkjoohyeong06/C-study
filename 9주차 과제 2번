#include <stdio.h>
char grade(int x){
    if(x>=90)
        return 'A';
    if(x>=80)
        return 'B';
    if(x>=70)
        return 'C';
    if(x>=60)
        return 'D';
    else
        return 'F';
}

int main(){
    int n;
    scanf("%d", &n);
    printf("%c", grade(n));
    return 0;
}
