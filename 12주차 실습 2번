#include <stdio.h>

enum Month {
    Jan = 1,
    Feb,
    Mar,
    Apr,
    May,
    Jun,
    Jul,
    Aug,
    Sep,
    Oct,
    Nov,
    Dec,
    MonthCount  // 13이 됨 (Dec + 1)
};

int main(void) {
    int i = 1;
    for(i = Jan; i < MonthCount; i++) 
        printf("%d ", i);
    return 0;
}
